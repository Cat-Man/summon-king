# 地图组合解锁与导流 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 把地图副本准入从单一灵力门槛升级为组合成长条件，并在地图页明确告诉玩家缺什么、该去哪里补。

**Architecture:** 后端继续沿用 `dungeon` 模块作为地图与准入规则的唯一来源，只新增最小组合门槛字段并收口到 `EnterDungeon()` 校验。前端不增加新接口，而是复用地图 overview 和 growth wallet，直接在地图页计算缺失条件与推荐去向，给出跳往修行/战骨/魔魂页的 CTA。

**Tech Stack:** Go 1.22, Gin, Vue 3, TypeScript, Vue Router, Vitest

### Task 1: 后端地图准入升级为组合条件

**Files:**
- Modify: `apps/backend/internal/modules/dungeon/domain.go`
- Modify: `apps/backend/internal/modules/dungeon/repository.go`
- Modify: `apps/backend/internal/modules/dungeon/service.go`
- Modify: `apps/backend/internal/modules/dungeon/service_test.go`

**Step 1: Write the failing tests**

在 `apps/backend/internal/modules/dungeon/service_test.go` 增加：

```go
func TestGetWorldMap_IncludesCompositeUnlockRequirements(t *testing.T) {
    svc := newTestDungeonService(t)

    world, err := svc.GetWorldMap(context.Background())
    if err != nil {
        t.Fatalf("expected get world map success, got %v", err)
    }

    locked := world.Cities[0].Dungeons[1]
    if locked.UnlockBoneLevel != 2 {
        t.Fatalf("expected bone level 2, got %d", locked.UnlockBoneLevel)
    }
    if locked.UnlockSoulPieces != 3 {
        t.Fatalf("expected soul pieces 3, got %d", locked.UnlockSoulPieces)
    }
}

func TestEnterDungeon_RejectsWhenCompositeRequirementsNotMet(t *testing.T) {
    ctx := context.Background()
    growthRepo := growth.NewMemoryRepository()
    svc := NewService(NewMemoryRepository(), growthRepo)
    playerID := int64(1016)

    if err := growthRepo.UpdateSpiritPower(ctx, playerID, 20); err != nil {
        t.Fatalf("expected spirit update success, got %v", err)
    }

    _, err := svc.EnterDungeon(ctx, playerID, 2)
    if err != ErrDungeonLocked {
        t.Fatalf("expected ErrDungeonLocked, got %v", err)
    }
}

func TestEnterDungeon_AllowsWhenCompositeRequirementsAreMet(t *testing.T) {
    ctx := context.Background()
    growthRepo := growth.NewMemoryRepository()
    svc := NewService(NewMemoryRepository(), growthRepo)
    playerID := int64(1017)

    _ = growthRepo.UpdateSpiritPower(ctx, playerID, 20)
    _, _ = growthRepo.UpgradeBoneLevel(ctx, playerID, 1)
    _, _ = growthRepo.UpgradeSoulPower(ctx, playerID, 3)

    run, err := svc.EnterDungeon(ctx, playerID, 2)
    if err != nil {
        t.Fatalf("expected dungeon enter success, got %v", err)
    }
    if run.DungeonID != 2 {
        t.Fatalf("expected dungeon 2, got %d", run.DungeonID)
    }
}
```

**Step 2: Run tests to verify they fail**

Run:
```bash
cd apps/backend && go test ./internal/modules/dungeon -run 'TestGetWorldMap_IncludesCompositeUnlockRequirements|TestEnterDungeon_RejectsWhenCompositeRequirementsNotMet|TestEnterDungeon_AllowsWhenCompositeRequirementsAreMet' -v
```

Expected:
- FAIL because `MapDungeon` 还没有 `UnlockBoneLevel` / `UnlockSoulPieces`
- FAIL because `validateDungeonUnlock()` 还只校验灵力

**Step 3: Write minimal implementation**

- `MapDungeon` 新增：
  - `UnlockBoneLevel int`
  - `UnlockSoulPieces int`
- `repository.go` 给 `寒渊裂隙` 配最小组合门槛：
  - `unlock_spirit_power = 120`
  - `unlock_bone_level = 2`
  - `unlock_soul_pieces = 3`
- `service.validateDungeonUnlock()` 改为同时校验：
  - `wallet.SpiritPower`
  - `wallet.BoneLevel`
  - `wallet.SoulPieces`
- 不新增新错误类型，仍统一返回 `ErrDungeonLocked`

**Step 4: Run tests to verify they pass**

Run:
```bash
cd apps/backend && go test ./internal/modules/dungeon -run 'TestGetWorldMap_IncludesCompositeUnlockRequirements|TestEnterDungeon_RejectsWhenCompositeRequirementsNotMet|TestEnterDungeon_AllowsWhenCompositeRequirementsAreMet' -v
```

Expected: PASS

**Step 5: Commit**

```bash
git add apps/backend/internal/modules/dungeon
git commit -m "feat: add composite dungeon unlock rules"
```

### Task 2: 前端地图页展示锁定原因与成长去向

**Files:**
- Modify: `apps/game-web/src/api/modules/dungeon.ts`
- Modify: `apps/game-web/src/pages/maps/WorldMapPage.vue`
- Modify: `apps/game-web/src/pages/maps/__tests__/WorldMapPage.spec.ts`

**Step 1: Write the failing test**

在 `apps/game-web/src/pages/maps/__tests__/WorldMapPage.spec.ts` 扩 mock：

```ts
{
  dungeon_id: 2,
  dungeon_name: "寒渊裂隙",
  unlock_spirit_power: 120,
  unlock_bone_level: 2,
  unlock_soul_pieces: 3,
}
```

并增加断言：

```ts
expect(wrapper.text()).toContain("需灵力 120 / 战骨 2 / 魔魂 3")
expect(wrapper.text()).toContain("还差灵力 20")
expect(wrapper.text()).toContain("还差战骨 1")
expect(wrapper.text()).toContain("还差魔魂 3")
expect(wrapper.text()).toContain("去修行")
expect(wrapper.text()).toContain("去战骨")
expect(wrapper.text()).toContain("去魔魂")
```

并分别触发三个按钮，确认：

```ts
expect(push).toHaveBeenCalledWith({ name: "cultivation" })
expect(push).toHaveBeenCalledWith({ name: "growth-bone" })
expect(push).toHaveBeenCalledWith({ name: "growth-soul" })
```

**Step 2: Run test to verify it fails**

Run:
```bash
pnpm --dir apps/game-web test -- src/pages/maps/__tests__/WorldMapPage.spec.ts
```

Expected:
- FAIL because前端类型里没有新字段
- FAIL because地图页还没有锁定原因拆解和导流按钮

**Step 3: Write minimal implementation**

- `api/modules/dungeon.ts` 扩地图副本类型：
  - `unlock_bone_level`
  - `unlock_soul_pieces`
- `WorldMapPage.vue`：
  - 读取钱包中的 `spirit_power / bone_level / soul_pieces`
  - 计算每个副本的缺失条件列表
  - 保留“进入副本”按钮禁用态
  - 新增锁定原因区，展示：
    - 总门槛摘要
    - 每项缺口文案
  - 为缺失项生成 CTA：
    - 灵力不足 -> `/cultivation`
    - 战骨不足 -> `/growth/bone`
    - 魔魂不足 -> `/growth/soul`

**Step 4: Run test to verify it passes**

Run:
```bash
pnpm --dir apps/game-web test -- src/pages/maps/__tests__/WorldMapPage.spec.ts
```

Expected: PASS

**Step 5: Commit**

```bash
git add apps/game-web/src/api/modules/dungeon.ts apps/game-web/src/pages/maps
git commit -m "feat: guide locked map entries to growth pages"
```

### Task 3: 任务3聚焦验证

**Files:**
- No code changes expected

**Step 1: Run focused verification**

Run:
```bash
cd apps/backend && go test ./internal/modules/dungeon -v
pnpm --dir apps/game-web test -- src/pages/maps/__tests__/WorldMapPage.spec.ts
```

**Step 2: Run broader verification**

Run:
```bash
make test-backend
make test-frontend
```

Expected: PASS

**Step 3: Review workspace**

Run:
```bash
git status --short
```

Expected:
- 只新增本任务相关改动
- 不提交旧未跟踪文件 `docs/plans/2026-04-02-底座与首条闭环实施计划.md`
