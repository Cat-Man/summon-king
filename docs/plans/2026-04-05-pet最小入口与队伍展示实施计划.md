# pet 最小入口与队伍展示 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为当前最小 `pet` 骨架补一个可访问的玩家端入口，让首页阵容卡和导航都能进入独立幻兽页，并展示“战斗队模块 + 幻兽栏模块”的最小只读结构。

**Architecture:** 后端继续复用现有 `pet` 内存仓储，不扩完整图鉴、技能、养成和持久化，只新增一个玩家端查询接口，返回当前战斗队和最小幻兽栏视图。前端新增 `PetPage`，优先展示战斗队摘要和 starter 幻兽卡片，并从首页与导航接入。当前阶段不做真正上下阵编辑，只把结构和契约先立住，为下一批编辑能力留入口。

**Tech Stack:** Go 1.22, Gin, Vue 3, TypeScript, Vitest

### Task 1: 新增后端 pet 玩家端查询接口

**Files:**
- Modify: `apps/backend/internal/modules/pet/domain.go`
- Modify: `apps/backend/internal/modules/pet/repository.go`
- Modify: `apps/backend/internal/modules/pet/service.go`
- Create: `apps/backend/internal/modules/pet/http.go`
- Create: `apps/backend/internal/modules/pet/http_test.go`
- Modify: `apps/backend/internal/modules/pet/service_test.go`
- Modify: `apps/backend/internal/bootstrap/router.go`
- Modify: `apps/backend/internal/bootstrap/router_test.go`

**Step 1: Write the failing tests**

在 `apps/backend/internal/modules/pet/service_test.go` 追加：

```go
func TestService_GetCollectionReturnsBattleTeamAndRoster(t *testing.T) {
    ctx := context.Background()
    svc := NewService(NewMemoryRepository())

    data, err := svc.GetCollection(ctx, 1001)

    if err != nil {
        t.Fatalf("expected collection success, got %v", err)
    }
    if data.PlayerID != 1001 {
        t.Fatalf("expected player 1001, got %d", data.PlayerID)
    }
    if data.TeamSize != 1 {
        t.Fatalf("expected team size 1, got %d", data.TeamSize)
    }
    if len(data.ActiveTeam) != 1 {
        t.Fatalf("expected active team 1, got %d", len(data.ActiveTeam))
    }
    if len(data.Roster) != 1 {
        t.Fatalf("expected roster 1, got %d", len(data.Roster))
    }
}
```

在 `apps/backend/internal/modules/pet/http_test.go` 写：

```go
func TestHandler_TeamReturnsPetCollection(t *testing.T) {
    gin.SetMode(gin.TestMode)

    r := gin.New()
    r.Use(middleware.InjectTraceID())
    h := NewHandler(NewService(NewMemoryRepository()))
    g := r.Group("/pet")
    h.RegisterRoutes(g)

    req := httptest.NewRequest(http.MethodGet, "/pet/team?player_id=1001", nil)
    req.Header.Set("X-Trace-ID", "trace-pet-team")
    resp := httptest.NewRecorder()
    r.ServeHTTP(resp, req)

    var payload struct {
        Code int `json:"code"`
        Data CollectionView `json:"data"`
    }
    if err := json.NewDecoder(resp.Body).Decode(&payload); err != nil {
        t.Fatalf("expected JSON payload, got %v", err)
    }
    if resp.Code != http.StatusOK {
        t.Fatalf("expected 200, got %d", resp.Code)
    }
    if payload.Data.TeamSize != 1 {
        t.Fatalf("expected team size 1, got %d", payload.Data.TeamSize)
    }
}
```

在 `apps/backend/internal/bootstrap/router_test.go` 把 `/api/v1/pet` 加进模块根测试。

**Step 2: Run tests to verify they fail**

Run:
```bash
cd apps/backend && go test ./internal/modules/pet ./internal/bootstrap -v
```

Expected: FAIL because `GetCollection`、`CollectionView`、`pet` handler 还不存在

**Step 3: Write minimal implementation**

- `domain.go`
  - 新增 `CollectionView`
  - 字段至少包含：`player_id`、`total_power`、`team_size`、`active_team`、`roster`
- `repository.go`
  - 新增 `ListPets(ctx, playerID)`，最小内存实现直接返回 starter 幻兽
- `service.go`
  - 新增 `GetCollection(ctx, playerID)`
  - 复用 `GetBattleTeam` 汇总 `total_power`
  - `active_team` 来自 battle team，`roster` 来自 `ListPets`
- `http.go`
  - `GET /team?player_id=`
  - 返回 `CollectionView`
- `router.go`
  - 注册 `/api/v1/pet` 和模块根

**Step 4: Run tests to verify they pass**

Run:
```bash
cd apps/backend && go test ./internal/modules/pet ./internal/bootstrap -v
```

Expected: PASS

**Step 5: Commit**

```bash
git add apps/backend/internal/modules/pet apps/backend/internal/bootstrap
git commit -m "feat: add minimal pet collection api"
```

### Task 2: 新增前端 pet 页面并接入导航/首页

**Files:**
- Create: `apps/game-web/src/api/modules/pet.ts`
- Create: `apps/game-web/src/pages/pet/PetPage.vue`
- Create: `apps/game-web/src/pages/pet/__tests__/PetPage.spec.ts`
- Modify: `apps/game-web/src/router/index.ts`
- Modify: `apps/game-web/src/router/__tests__/routes.spec.ts`
- Modify: `apps/game-web/src/layouts/GameLayout.vue`
- Modify: `apps/game-web/src/pages/home/HomePage.vue`

**Step 1: Write the failing tests**

在 `apps/game-web/src/router/__tests__/routes.spec.ts` 追加：

```ts
test("contains pet route", () => {
  const hasRoute = router.getRoutes().some((route) => route.path === "/pet")

  expect(hasRoute).toBe(true)
})
```

在 `apps/game-web/src/pages/pet/__tests__/PetPage.spec.ts` 写：

```ts
test("renders active team and roster from api", async () => {
  vi.mocked(getPetCollection).mockResolvedValue({
    player_id: 1001,
    total_power: 120,
    team_size: 1,
    active_team: [{ pet_id: 10011, slot: 1, name: "初始灵狐", level: 1, power: 120, is_active: true }],
    roster: [{ pet_id: 10011, slot: 1, name: "初始灵狐", level: 1, power: 120, is_active: true }],
  })

  const wrapper = mount(PetPage, { ... })
  await flushPromises()

  expect(wrapper.text()).toContain("战斗队")
  expect(wrapper.text()).toContain("幻兽栏")
  expect(wrapper.text()).toContain("初始灵狐")
  expect(wrapper.text()).toContain("120")
})
```

**Step 2: Run tests to verify they fail**

Run:
```bash
pnpm --dir apps/game-web test -- PetPage routes
```

Expected: FAIL because `pet.ts`、`PetPage.vue` 和 `/pet` route 还不存在

**Step 3: Write minimal implementation**

- `api/modules/pet.ts`
  - 声明 `PetCollection`、`BattlePet`
  - `getPetCollection(playerId)`
- `pages/pet/PetPage.vue`
  - 页面结构至少包含：
    - 战斗队摘要区
    - 幻兽栏列表区
    - 到 `战灵 / 战骨 / 魔魂` 的养成 CTA
  - 加载 `/api/v1/pet/team`
- `router/index.ts`
  - 新增 `/pet`
- `GameLayout.vue`
  - 功能导航新增 `幻兽`
- `HomePage.vue`
  - 首页阵容卡 CTA 改为 `/pet`

**Step 4: Run tests to verify they pass**

Run:
```bash
pnpm --dir apps/game-web test -- PetPage routes HomePage
```

Expected: PASS

**Step 5: Commit**

```bash
git add apps/game-web/src/api/modules/pet.ts apps/game-web/src/pages/pet apps/game-web/src/router apps/game-web/src/layouts/GameLayout.vue apps/game-web/src/pages/home/HomePage.vue
git commit -m "feat: add minimal pet page entry"
```

### Task 3: 全量验证 pet 入口闭环

**Files:**
- No new files; verification only

**Step 1: Run backend verification**

Run:
```bash
make test-backend
```

Expected: PASS

**Step 2: Run frontend verification**

Run:
```bash
make test-frontend
```

Expected: PASS

**Step 3: Run backend smoke build**

Run:
```bash
make smoke-backend
```

Expected: PASS

**Step 4: Push**

```bash
git push origin feat/foundation-first-loop
```
